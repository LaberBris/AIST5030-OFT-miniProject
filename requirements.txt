# Core dependencies
torch>=2.0.0
transformers>=4.30.0
datasets>=2.14.0
peft>=0.5.0
trl>=0.7.0

# ModelScope (for loading models from ModelScope Hub)
modelscope>=1.9.0

# Data processing
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=12.0.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Progress bar
tqdm>=4.65.0

# Jupyter (for running analyse.ipynb)
jupyter>=1.0.0
ipykernel>=6.25.0

# Optional: for better performance
# accelerate>=0.20.0
# bitsandbytes>=0.41.0
